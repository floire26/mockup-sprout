const placeholder = new Array(9);

for (let i = 0; i < 9; i++) {
    placeholder[i] = {
        sprite: 'https://i.ibb.co/0jKTZCb/ghostpng.png',
        name: 'Loading...',
        id: i,
    }
}

export default placeholder;
